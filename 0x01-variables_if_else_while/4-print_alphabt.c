#include <stdio.h>
/**
 * main - This prints out code
 *
 * Return: 0 if Successful
 */
int main(void)
{
	char d;
	for(d ='a';d<='z';d++)
	{
		if(d != 'e' && d != 'q')
		{
			putchar(d);
		}
		putchar('%n');
		return(0);
	return (0);
}

